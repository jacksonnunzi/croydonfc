name = "cms-auth"
main = "worker.js"
compatibility_date = "2024-01-01"

# Secrets set via `wrangler secret put`:
#   PASSWORD_HASH  - SHA-256 hex of the admin password
#   GITHUB_PAT     - Fine-grained GitHub personal access token
